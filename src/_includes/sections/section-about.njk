{# Data-driven — mirrors pages/about.njk. No hardcoded 725/175+/1,592/founders. #}
<section class="section" id="about" data-extra="1">
  <div class="container">
    <div class="section-header fade-up">
      <h2><i class="bi bi-info-circle"></i> {{ sections.about.title }}</h2>
      <div class="section-line"></div>
    </div>
    <div class="about-grid fade-up">
      <div class="about-text">
        <h3>{{ sections.about.subtitle }}</h3>
        {% for p in sections.about.paragraphs %}
          <p>{{ p }}</p>
        {% endfor %}
        <div class="about-stats">
          {% for s in sections.about.stats %}
          <div class="stat-box">
            <div class="num">{{ s.num }}</div>
            <div class="label">{{ s.label }}</div>
          </div>
          {% endfor %}
        </div>
      </div>
      <div class="about-visual" style="background-image:url('/images/drone_view_1.jpg')">
        <div class="about-visual-overlay"></div>
      </div>
    </div>
    {#- מפה: היה iframe חי ל-google.com/maps — קריאה חיצונית שנחסמת בסינון תוכן.
        הוחלף בכרטיס מקומי עם קישור יוצא, כדי שהעמוד ייטען במלואו ללא תלות חיצונית. #}
    <div class="map-container fade-up">
      <div class="map-static">
        <h3><i class="bi bi-geo-alt-fill" aria-hidden="true"></i> מיקום היישוב</h3>
        <p>מעלה עמוס · דרום-מזרח גוש עציון, כ-20 ק"מ דרומית-מזרחית לירושלים. הגישה דרך צומת מצד.</p>
        {%- if sections.about.mapUrl %}
        <p><a class="btn btn-primary" href="{{ sections.about.mapUrl | safeUrl }}" target="_blank" rel="noopener noreferrer">פתיחת המפה בכרטיסייה חדשה</a></p>
        {%- endif %}
      </div>
    </div>
  </div>
</section>
