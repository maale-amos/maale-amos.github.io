# תוכנית עבודה — אתר קהילת מעלה עמוס · 8 שלבים

**עודכן:** 19.07.2026 · **מבוסס על:** קריאה עמוקה של אקוסיסטם מעלה עמוס (`C:\projects\maale-amos`), ביקורת תוכן+קוד של האתר, ומחקר רשת מאמת.
**מצב פתיחה:** האתר בנוי, פרוס וחי ב-[maale-amos.github.io](https://maale-amos.github.io) (Eleventy + Cloudflare Worker + D1/KV/Drive). הפער אינו "לבנות מאפס" אלא **להשלים, לדייק ולהעביר לייצור מלא**.

---

## מקרא

| סימון | משמעות |
|------|---------|
| ✅ | בוצע בסשן הזה (19.07.2026) |
| 🔧 | משימה ל-Claude (קוד/תוכן — אוטונומי) |
| 👤 | משימה ליוסף בלבד (כסף, דומיין, סודות, אישורים) |
| 🔴🟠🟡 | עדיפות: קריטית / גבוהה / רגילה |

---

## שלב 1 — היגיינת אבטחה וסודות 🔴

**מטרה:** לסגור את הפער הקריטי של credential חשוף ולייצב את תשתית ה-auth לפני כל פריסה.

- 👤🔴 **סיסמת admin בטקסט גלוי** ב-`STATUS.md` וב-`DEPLOY_WHEN_DOMAIN_READY.md` (`YpCpnew@...`). אם קבצים אלה נדחפים לריפו הציבורי — חשיפת סיסמה. לוודא שהם ב-`.gitignore`/הוסרו מההיסטוריה, **ולסובב את הסיסמה מיד**.
- 👤 **Revoke** ל-refresh_token של Google שדלף בעבר מריפו vimeo-downloader (עדיין תקף ל-Drive של 6742853) — לפני חיבור Drive לקליטה.
- 🔧 לוודא `SESSION_KEY_HEX` מוגדר תקין (hex ≥64) ולתעד ב-runbook — אם חסר/לא-hex כל login מחזיר 500.
- 🔧 סריקת `secret-scanning`/היסטוריית git לוודא שאין סודות נוספים בריפו הפומבי.

---

## שלב 2 — תיקוני תוכן קריטיים (עובדות אמת) 🔴

**מטרה:** ליישר את האתר מול המקור הסמכותי — במיוחד תפקידים ושמות רשמיים, שהם קריטיים לאמון הקהילה.

- ✅ **איתן סער → "מנכ״ל היישוב"** (לא "מזכיר") — תוקן ב-`site.json` ובכל ה-footer.
- ✅ **מעון היום → "עמותת אלעד"** (תוקן מ-"אלעידא") + נוסף **הכולל המרכזי** בראשות הרב משה קיינרייך שליט״א (`education.json`).
- ✅ **רבש״ץ ר׳ יופה פנחס הי״ו** — הושלם שמו במקטע הביטחון (הוסר "בבדיקה").
- ✅ **"קהילתון" → "עיתון מעלה"** ו-**"ארגון החסד" → "קופת הקהילה"** (`community.json`).
- ✅ **אוטובוסים** — הוסרה תווית "בבדיקה"; נוסף מפעיל **אלקטרה אפיקים**, גישה דרך **צומת מצד**, ומוקד המועצה **02-9939933** (מאומת ברשת).
- ✅ נוסף מייל מזכירות כללי `office@maaleamos.org.il` ל-`site.json`.
- 🟡🔧 נותר לאמת מול המזכירות: מיקומי המקלטים וקיבולת (`emergency.json`), ושעות מוסדות החינוך (עדיין "בבדיקה").

---

## שלב 3 — מקטע הדגל: הנהלת הישוב + המרא דאתרא 🔴

**מטרה:** לסגור את הפער בעל הערך הגבוה ביותר — מבנה שלושת הגופים והדמות הרוחנית המרכזית, שנעדרו כליל.

- ✅ **`data/leadership.json`** נבנה במלואו — 12 בעלי תפקיד: מרא דאתרא הרב חרל״פ שליט״א, מנכ״ל, שני יו״רי הוועדים, ראש כולל, מנכ״ל קופה, רכז קהילה, רכזת תרבות, נציג במועצה, רבש״ץ ומהנדס. מרונדר בעמוד הבית.
- ✅ **המרא דאתרא** נוסף גם ל-`about.json` (פסקת אופי היישוב הישיבישי, "תורתם אומנותם", גישה דרך צומת מצד).
- 🟡🔧 נותר: להוסיף אזכור המרא דאתרא גם למקטע בתי הכנסת (`shuls`), ולשקול פריט ניווט ישיר ל"הנהלת הישוב".

---

## שלב 4 — העשרת מקטעי CMS ריקים בעובדות אמת 🟠

**מטרה:** למלא מקטעים ריקים בתוכן אמיתי כדי שהאתר ירגיש חי ולא "בבנייה". (מקטעים ריקים מתקפלים אוטומטית — לכן החלק הזה משנה מיידית את מראה העמוד.)

- ✅ **`data/newspaper.json`** — נזרע: עיתון "מעלה" (שבועי), 11 גיליונות אחרונים עד 231 (בהעלותך), מייל מערכת `maaleiton@gmail.com`.
- 🟠🔧 **`data/housing.json`** — פרויקטי הפיתוח: מבנה רב-תכליתי (תב״ר ~600 אלף ₪), מרכז מסחרי בתכנון, הרחבת קרית נוף, ספרייה מתחדשת.
- 🟡🔧 **stores / marketplace / gemachim / tzimer** — בית קלייה "בראשית" (ה'), מכירות אזוריות (רביעי 19:30–20:30 באולם), רשת החסד (חלוקות לחם, מלבושי כבוד, קמחא דפסחא).
- 🟡👤 **announcements / events / news / simchot** — תוכן שוטף שהמזכירות מזינה דרך ה-CMS (לא לזרוע נתונים מזויפים).

---

## שלב 5 — תיקוני קוד ו-QA פרונטאנד 🟠

**מטרה:** לנקות באגים קטנים ולוודא רינדור נקי בכל הדפים.

- ✅ **QA מלא בדפדפן** — 14 עמודים × דסקטופ+מובייל = **28/28 נקי** (קונסולה, כשלי רשת, גלישה אופקית, אלמנטי-סמוק). הרצה חוזרת אחרי כל תיקון.
- ✅ **תוקנה גלישה אופקית במובייל** (26px) שנוצרה מ-3 סטטים ב"אודות" — `about-stats` שונה ל-`minmax(0,1fr)`.
- ✅ תוקן קישור טלפון פגום בכרטיס הרבש״ץ.
- 🟡🔧 נותר: יישור ולידציית סיסמה לקוח(≥8)/שרת(≥10); תיקון גרשיים עבריים ב-`searchSite` (main.js:74) ומעבר ל-DOM-builder; ווידוא שכל קובצי `/images` בגלריה קיימים.

---

## שלב 6 — הפעלת שירותי Backend: Drive ומיגרציות 🟠

**מטרה:** להפעיל אחסון מסמכים לקליטה ולמנוע קריסות.

- 👤🟠 הרצת מיגרציה **`003_drive.sql`** מול D1 — כרגע `pending`; בלעדיה `/api/klita/export` מחזיר 500 לכל משתמש.
- 👤 הגדרת **Google OAuth + 3 secrets** (`GDRIVE_CLIENT_ID/SECRET/REFRESH_TOKEN`) + מילוי `KLITA_DRIVE_ROOT_ID` ו-`KLITA_COMMITTEE_EMAILS`.
- 🔧 תיקון אי-עקביות הרשאה: תפקיד `committee` יכול לייצא ת״ז מלאה דרך `?uid=` בעוד שאר ה-UI ממסך ל-4 ספרות — ליישר מיסוך. + אימות magic-bytes להעלאות (לא רק content_type מוצהר).

---

## שלב 7 — דומיין מותאם, מייל ופריסה מלאה 🟠

**מטרה:** מעבר מ-`workers.dev` לדומיין רשמי, הפעלת מייל, ונגישות מאחורי פילטרים.

- 👤🟠 רכישת **`maale-amos.org.il`** + חיבור nameservers ל-Cloudflare + SSL Full(strict).
- 👤 Custom Domain ל-Worker (`api.maale-amos.org.il`), `workers_dev=false`, עדכון `src/js/config.js`.
- 🔧 הפעלת **`MAIL_ENABLED`** + חיבור transport אמיתי (Cloudflare Email Routing/SES) → תזכורות ואישורים לנקלטים.
- 🔧 `wrangler deploy` לשינויים הלא-פרוסים (Z14–Z18) — הגרסה החיה כרגע ישנה מהקוד.
- 👤 הגשת בקשות אישור לנטפרי/רימון/אתרוג (נוסח מוכן ב-`DEPLOY_WHEN_DOMAIN_READY.md`).

---

## שלב 8 — הקשחה, גיבוי ותחזוקה שוטפת 🟡

**מטרה:** להעביר את האתר למצב ייצור בר-קיימא.

- 🔧 שדרוג **rate-limit** ל-Durable Object/Rate-Limiting binding (במקום KV לא-אטומי — חלש נגד burst).
- 👤 הגדרת **גיבוי D1 שבועי** (מפתחות age + Cloudflare API token) — ראה `docs/D1_BACKUP.md`.
- 👤 החלפת סיסמת admin ראשונית + הדרכת המזכירות לניהול תוכן שוטף דרך ה-CMS.
- 🔁 **תחזוקה חיה:** הזנת announcements/events/news/simchot והוספת גיליון עיתון שבועי — זה מה שישמור על האתר "חי".

---

## תלויות בין השלבים

```
שלב 1 (אבטחה) ─┐
שלב 2 (תוכן) ──┼─► שלב 5 (QA) ─► שלב 6 (Backend) ─► שלב 7 (דומיין+מייל) ─► שלב 8 (הקשחה)
שלב 3 (הנהלה) ─┤                    ▲
שלב 4 (CMS) ───┘                    └── תלוי במיגרציה 003 + OAuth (יוסף)
```

שלבים 1–5 הם תוכן/קוד ורובם כבר בוצעו או ניתנים לביצוע ע"י Claude. שלבים 6–8 חוסמים על פעולות של יוסף (כסף, דומיין, סודות Google, אישורי פילטרים).

---

## נספח א' — עובדות מפתח מאומתות (מקור לתוכן)

**היישוב:** גוש עציון, דרום-מזרח הרי יהודה, ~20 ק״מ מדרום-מזרח לירושלים, גובה ~748 מ׳. נוסד 1981 (היישוב החרדי הראשון בגוש), על שם הנביא עמוס. ~1,340 תושבים (2024). גישה דרך צומת מצד. *(מקורות: hamichlol, myesha, goyosh — ודאות גבוהה)*

**מבנה ניהול (3 גופים):** האגודה השיתופית (כלכלי, יו״ר אולסקר) · הוועד המקומי (מוניציפלי, יו״ר גלסנר) · המועצה האזורית גוש עציון (רשות ממונה, נציג ארה כהן; מוקד 02-9939933).

**אוטובוסים:** קו 365 מעלה עמוס↔ירושלים · קו 411 ביתר עילית↔אספר דרך מעלה עמוס · מפעיל: אלקטרה אפיקים.

**עיתון:** "מעלה" — שבועי (שישי), ~30–34 עמ׳, גיליון אחרון 231 (29.05.2026), מערכת `maaleiton@gmail.com`.

## נספח ב' — אתרים אחיים באקוסיסטם (למידע)

- **`zimmer-maale-amos`** — אתר צימר (index/admin/gallery, git) — עצמאי.
- **`cheder-maale-amos`** — מערכת מעקב התנהגות לת״ת (paused, ממתין להפעלת עמנואל).
- **`C:\projects\maale-amos`** — אקוסיסטם התוכן: master_doc, מאגר עיתון, NotebookLM לכתיבת כתבות.
